pipeline "MyMLPipeline";

// origem do dataset
dataset Petals1 "Petals Dataset":
  source = path
  preprocess
      -> remove('columnName')
      -> normalize('columnName')
  visualize = {
    histogram('all'),
    correlation('all')
  }
  split:
    train = 0.8
    test = 0.2