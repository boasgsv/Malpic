pipeline "MyMLPipeline";

// origem do dataset
dataset Petals1 "Petals Dataset":
  source = "path"
  preprocess
    remove("columnName")
    -> normalize("columnName")
  visualize {
    histogram,
    correlation
  }
  split all as f1, f2, f3, f4 0.1, 0.2, 0.6, 0.1